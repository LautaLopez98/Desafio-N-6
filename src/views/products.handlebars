<h1>Productos</h1>
<header>
    <nav>
        <ul class="nav">
            <li>
                <a href="/">Home</a>
            </li>
            <li>
                <a href="/realTimeProducts">Real Time Products</a>
            </li>
            <li>
                <a href="/carts/663abad4c100e248d80f3373">Carrito</a>
            </li>
            <li>
                <a href="/chats">Chat</a>
            </li>
        </ul>
    </nav>
    <hr />
</header>
<hr>
<input type="text" value="{{cart._id}}" hidden id="cart">
<div class="container">
    {{#each productos}}
    <div class="card">
            <h3>{{title}}</h3>
            <img src="/uploads/{{thumbnail}}" alt="Thumbnail"></p>
            <p>Precio: ${{price}}</p>
            <p>Código: {{code}}</p>
            <p>Stock: {{stock}}</p>
            <p>Category: {{category}}</p>
            <p>Descripción:{{description}}</p>
            <p>Status: {{status}}</p>
        <button class="boton" onclick="comprar('{{_id}}')">Agregar a Carrito</button>
    </div>
    {{/each}}
</div>
<br>
<div class="container">
    <a href="/products?page=1">Página N°1</a>

    {{#if hasPrevPage}}
        <a href="/products?page={{prevPage}}">Página anterior</a>
    {{/if}}

    {{#if hasNextPage}}
        <a href="/products?page={{nextPage}}">Página siguiente</a>
    {{/if}}

    <a href="/products?page={{totalPages}}">Última página</a>
    <br>
    <label for="paginaActual"></label>
</div>


<script src="/js/products.js"></script>


